<%= form_for [:admin, @user], url: move_team_admin_management_user_path, html: {class: "form-inline", remote: true} do |f| %>
  <%= collection_select :user, :team_id, @teams, :id, :name, {}, {class: "form-control"} %>
  <%= f.submit t("admin.users.user.update") ,class: "btn btn-info" %>
<% end %>
